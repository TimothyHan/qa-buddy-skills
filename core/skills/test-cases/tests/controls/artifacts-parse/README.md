<!-- rubric-control: criterion=artifacts-parse case=projects-happy expect=below-floor -->
A mapping with the comma between two mapping entries missing — the exact defect PR #87 fixed in production. `artifacts-parse` must score 0 on this directory; the document that would accompany it is irrelevant to the criterion.
